# Legesgue integral
1. we first condsider the case of simple function(c.f.[[可测函数]]) $\phi$ is a finite sum $\phi(x) = \sum_{k=1}^Na_k \chi_{E_k}(x)$, where $E_k$ are measurable sets of finite measure and the $a_k$ are constants. A complication of this definition is the lack of uniqueness, ($0=\chi_{E}-\chi_{E}$ for example). This can be solved by defining a canonical form of $\phi$ by forcing $E_k$ to be disjoint. Now we let $\int_{\mathbb{R}^d}\phi(x)dx=\sum_{k=1}^M c_km(F_k)$$ 